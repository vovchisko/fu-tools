# isNaN(value)

Checks if value is NaN. Alias for `Number.isNaN()`.

```javascript
import { isNaN } from 'fu-tools'

isNaN(NaN)           // true
isNaN(42)            // false
isNaN('not number')  // false
```